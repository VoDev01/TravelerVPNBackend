---
- name: Verify Compose Application Stack
  hosts: localhost
  gather_facts: false
  tasks:
    - name: Check if web service endpoint responds correctly
      ansible.builtin.uri:
        url: "http://localhost:8080"
        status_code: 200
      register: result
      until: result.status == 200
      retries: 5
      delay: 2
