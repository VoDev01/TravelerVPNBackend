spring:
  cloud:
    vault:
      host: vault
      port: 8200
      scheme: http
      authentication: CUBBYHOLE
      fail-fast: true
      token: ${SPRING_CLOUD_VAULT_TOKEN}
      kv:
        enabled: true